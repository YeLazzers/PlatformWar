public enum AbilitySlot
{
    Primary,
    Secondary,
    Ultimate
}

public enum AbilityExecutionPolicy
{
    Instant,
    Channeled,
    Aura
}

public enum AbilityTargetingPolicy
{
    Self,
    Target,
    NonTarget,
    Directional,
}